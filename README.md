# Test Assignment for "Which?"

This is a homework coding test assignment made for **Which?**

## Starting the Application

To start the server, run the following command in the terminal:

`$ npm start`

## Run the Tests

In order to run the tests, run the following command in the terminal:

`$ npm test`
